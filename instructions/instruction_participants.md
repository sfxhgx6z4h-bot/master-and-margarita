# Редакция Мастера — Инструкция участника

> У тебя уже есть аккаунт на GitHub — начинаем работать
> Репозиторий: https://github.com/imagislrus/master-and-margarita

---

## Как выглядит весь процесс

```
Fork → Clone → Ветка → Работа с файлом → Commit → Push → Pull Request
```

Звучит много — но после второго раза делается автоматически за 2 минуты.

---

## Шаг 1 — Сделать Fork

Fork — это твоя личная копия общего репозитория на GitHub. Ты работаешь в своей копии, а потом предлагаешь изменения в общую через Pull Request.

> 💡 **Аналогия:** общий репо — оригинал книги. Fork — твоя личная копия. Pull Request — «хочу добавить свои правки в оригинал».

**1.** Перейди в репозиторий проекта: https://github.com/imagislrus/master-and-margarita

**2.** Нажми кнопку **Fork** — она в правом верхнем углу страницы

**3.** Нажми **Create fork** — GitHub создаст копию на твоём аккаунте. Адрес станет: `github.com/ТВОЁ-ИМЯ/master-and-margarita`

> ✅ Fork делается **один раз** в начале проекта. Для следующих задач он уже есть — начинай сразу с Шага 2.

---

## Шаг 2 — Клонировать репозиторий на компьютер

Репо существует на GitHub — в интернете. Нужно скачать его к себе на компьютер. Это называется **clone**.

**1.** На странице своего форка нажми зелёную кнопку **Code**
Убедись что ты на СВОЁМ форке: `github.com/ТВОЁ-ИМЯ/...` — не на оригинале

**2.** Выбери **HTTPS** и скопируй ссылку

**3.** Открой терминал и перейди в нужную папку:

```bash
# Windows — открой командную строку (Win+R → cmd → Enter):
cd C:\Users\ТвоёИмя\Desktop

# Mac — открой Terminal (Cmd+Пробел → Terminal → Enter):
cd ~/Desktop
```

**4.** Выполни команду clone:

```bash
git clone https://github.com/ТВОЁ-ИМЯ/master-and-margarita.git
```

**5.** Перейди в папку проекта:

```bash
cd master-and-margarita
```

> ✅ Clone тоже делается **один раз**. Папка появится на компьютере — можешь открыть её в проводнике.

---

## Шаг 3 — Создать свою ветку

Ветка — это твоё отдельное рабочее пространство. **Главное правило: никогда не работай напрямую в main. Всегда создавай свою ветку.**

> 💡 **Аналогия:** main — чистовик команды. Твоя ветка — черновик. Черновик проверяется, потом переносится в чистовик.

Перед созданием ветки — убедись что у тебя свежая версия main:

```bash
git checkout main
git pull origin main
```

Создай новую ветку:

```bash
git checkout -b feature/имя-задачи

# Примеры:
git checkout -b feature/woland-character
git checkout -b feature/moscow-location
```

Проверь что ты на нужной ветке — текущая отмечена звёздочкой `*`:

```bash
git branch
```

---

## Шаг 4 — Выполнить задание

Делай своё задание — создай или отредактируй нужный файл. Открой папку проекта в любом текстовом редакторе. Рекомендуем **VS Code** (code.visualstudio.com — бесплатно).

Открыть папку проекта в VS Code из терминала:

```bash
code .
```

### Формат файлов — Markdown (.md)

Все файлы в проекте — `.md` файлы. Вот основное что нужно знать:

```markdown
# Заголовок первого уровня
## Заголовок второго уровня
### Заголовок третьего уровня

Обычный текст пишется просто так.

**Жирный текст** — две звёздочки с двух сторон
*Курсив* — одна звёздочка

- Пункт списка
- Ещё один пункт

---   (горизонтальная линия)
```

> ✅ Не нужно знать Markdown идеально. Просто пиши текст с заголовками — этого достаточно.

---

## Шаг 5 — Сохранить изменения (commit)

Commit — это точка сохранения с описанием что ты сделал.

**1.** Посмотри что изменилось:

```bash
git status
```

**2.** Добавь файлы в коммит:

```bash
# Добавить конкретный файл:
git add characters/woland.md

# Или все изменения сразу:
git add .
```

**3.** Сделай коммит с описанием:

```bash
git commit -m "Добавил карточку персонажа Воланда"
```

> ⚠️ Пиши описание по-человечески — что именно ты сделал.
> **Хорошо:** «Добавил описание Воланда», «Исправил опечатки в moscow.md»
> **Плохо:** «изменения», «правки», «update»

---

## Шаг 6 — Отправить на GitHub (push)

Коммит пока только у тебя на компьютере. Отправь его на GitHub:

```bash
git push origin feature/имя-твоей-ветки
```

> ✅ Первый push может попросить авторизацию — войди в аккаунт GitHub. На Windows может открыться браузер — это нормально, просто войди.

---

## Шаг 7 — Открыть Pull Request

Pull Request (PR) — это твоё предложение добавить изменения в общий проект.

**1.** Перейди на страницу своего форка: `github.com/ТВОЁ-ИМЯ/master-and-margarita`

**2.** Нажми кнопку **Compare & pull request**
Она появится автоматически после push. Если нет — нажми вкладку Pull requests → New pull request

**3.** Заполни заголовок и описание PR:

```
Заголовок: Добавил карточку персонажа Воланда

Описание:
Что сделал: создал файл characters/woland.md
Что внутри: имя, роль в романе, краткое описание персонажа

Closes #1   ← если была Issue, укажи её номер так
```

**4.** Нажми **Create pull request** — готово! Мейнтейнер получит уведомление.

> ✅ После создания PR не удаляй ветку и не делай новые коммиты пока не получишь ответ.
> Если мейнтейнер попросил правки — внеси их, сделай новый commit и push в ту же ветку. PR обновится автоматически.

---

## Шаг 8 — Следить за изменениями других (fetch и pull)

> ⚠️ **Правило:** перед началом каждой новой задачи — сначала fetch и pull. Иначе будешь работать со старой версией и получишь конфликт.

```bash
# 1. Посмотри что изменилось (ничего не применяет — только смотрит):
git fetch origin

# 2. Переключись на main:
git checkout main

# 3. Подтяни свежие изменения:
git pull origin main

# 4. Создай новую ветку для следующей задачи:
git checkout -b feature/следующая-задача
```

---

## Шаг 9 — Что делать если возник конфликт

> ✅ **Конфликт — это не ошибка и не катастрофа. Это нормальная рабочая ситуация. Все через это проходят.**

Когда есть конфликт, Git добавляет в файл маркеры:

```
<<<<<<< HEAD
Это твоя версия текста
=======
Это версия из main — то что написал другой участник
>>>>>>> main
```

### Как решить — 4 шага:

**1.** Открой файл с конфликтом в VS Code
VS Code подсветит конфликт и покажет кнопки: **Accept Current Change / Accept Incoming Change / Accept Both Changes**

**2.** Реши что оставить:
- Accept Current — оставить свою версию
- Accept Incoming — оставить чужую
- Accept Both — оставить обе (добавятся друг за другом)

**3.** Убедись что маркеры удалены — в файле не должно быть строк с `<<<<<<`, `=======`, `>>>>>>>`. Должен остаться только чистый текст.

**4.** Сохрани, сделай commit и push:

```bash
git add .
git commit -m "Разрешил конфликт в файле woland.md"
git push origin feature/имя-ветки
```

### Способ 2 — через git rebase (если PR показывает конфликт)

Если твой PR показывает конфликт — значит кто-то изменил тот же файл раньше тебя. Сделай так:

**1.** Обнови main и накати свои изменения поверх:
```bash
git checkout main
git pull origin main
git checkout твоя-ветка
git rebase main
```

**2.** Открой файл с конфликтом — убери маркеры `<<<<<<<`, `=======`, `>>>>>>>`

**3.** Сохрани файл и продолжи rebase:
```bash
git add .
git rebase --continue
git push origin твоя-ветка --force
```

> ⚠️ `--force` нужен только после rebase — в других случаях не используй.
После вставки нажми Commit changes прямо в main.

> ⚠️ Если совсем непонятно что делать — не трогай файл, напиши в чат команды. Мейнтейнер разберёт вместе с тобой.

---

## Шпаргалка — весь цикл работы

```
Начало каждой новой задачи:
git checkout main → git pull origin main → git checkout -b feature/название

После написания файла:
git add . → git commit -m 'описание' → git push origin feature/название

Затем открыть Pull Request на GitHub — и ждать Review.
```

### Все команды

| Что нужно сделать | Команда |
|---|---|
| Посмотреть текущую ветку | `git branch` |
| Создать и переключиться на ветку | `git checkout -b feature/название` |
| Переключиться на main | `git checkout main` |
| Посмотреть что изменилось | `git status` |
| Добавить все файлы в коммит | `git add .` |
| Добавить конкретный файл | `git add путь/к/файлу.md` |
| Сделать коммит | `git commit -m "что сделал"` |
| Отправить ветку на GitHub | `git push origin feature/ветка` |
| Узнать об изменениях других | `git fetch origin` |
| Подтянуть изменения в main | `git pull origin main` |
| Посмотреть историю коммитов | `git log --oneline` |
| Спрятать незаконченную работу | `git stash` |
| Вернуть спрятанное | `git stash pop` |
| Откатить последний коммит | `git revert HEAD` |

### Частые ошибки

| Ошибка или ситуация | Что делать |
|---|---|
| `error: failed to push — rejected` | Кто-то изменил файл раньше тебя. Сделай `git pull` и реши конфликт |
| `nothing to commit, working tree clean` | Ты ничего не изменил или забыл сохранить файл в редакторе |
| `Merge conflict in файл.md` | Открой файл, найди маркеры `<<<`, реши конфликт, сделай commit |
| `HEAD detached at...` | Сделай `git checkout main` — ты случайно вышел из ветки |
| Забыл название своей ветки | Введи `git branch` — список всех веток со звёздочкой у текущей |
| PR не принимают, просят правки | Внеси правки, сделай commit и push — PR обновится сам |
| Случайно работал в main | Не паникуй — напиши мейнтейнеру, разберётесь вместе |

---

> 🔴 **Три главных правила:**
> 1. Перед началом задачи — всегда `git pull` чтобы получить свежую версию
> 2. Работай только в своей ветке — никогда напрямую в main
> 3. Конфликт — это нормально. Читай маркеры, не паникуй, зови на помощь если нужно
